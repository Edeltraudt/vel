// JSDoc is the source of truth; this generates custom-elements.json — the
// contract read by docs, editors/types, and the future MCP server.
export default {
  globs: ["src/**/*.js"],
  exclude: ["src/**/*.test.js"],
  outdir: ".",
};
