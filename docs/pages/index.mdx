---
layout: ../layouts/Layout.astro
title: vel — showcase
---

# vel — showcase (stub)

Step-3 placeholder. `npm run dev` serves this with HMR; flesh out one section per
component. This is _showcase + reference_, not test coverage — the rigor lives in
the `@web/test-runner` specs.

## Toast

<vel-toaster id="demo-toaster" duration="4000"></vel-toaster>

<button
	class="vel-button"
	onclick="document.getElementById('demo-toaster').toast('Saved', { tone: 'success' })"
>
	{"Fire a toast"}
</button>
